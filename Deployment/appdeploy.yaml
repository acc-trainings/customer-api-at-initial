# Service and Deployment configuration for Customer service deployment.

##################################################################################################
# Customer service
##################################################################################################
apiVersion: v1
kind: ConfigMap
metadata:
  name: customer-api
data:
  #spring.profiles.active: local
  spring.data.mongodb.uri: mongodb+srv://demo123:demo123@acctrainings.dntd4.mongodb.net/db-acc-trainings?retryWrites=true&w=majority
  spring.data.mongodb.database: db-acc-trainings
  #datastore.mongodb.collection.name: customer-coll-local
---
apiVersion: v1
kind: Service
metadata:
  name: acctrainings-ketan
  namespace: "acctrainings-ketan"
  labels:
    app: acctrainings-ketan
    service: acctrainings-ketan
spec:
  ports:
  - port: 8080
    name: http
  selector:
    app: acctrainings-ketan-node
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: acctrainings-ketan
  labels:
    app: acctrainings-ketan
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: acctrainings-ketan-node
      version: v1
  template:
    metadata:
      labels:
        app: acctrainings-ketan-node
        version: v1
    spec:
      containers:
      - name: acctrainings-ketan-node
        image: quay.io/acctrainings/customer-api:%%IMG_TAG%%
        imagePullPolicy: IfNotPresent
        envFrom:
          - configMapRef:
              name: customer-api
        ports:
        - containerPort: 8080
        env:
        resources: {}
      imagePullSecrets:
        - name: quay-cred       
---
apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: acctrainings-ketan
  namespace: "acctrainings-ketan"
  labels:
    app: acctrainings-ketan
spec:
  to:
    kind: Service
    name: acctrainings-ketan
status:
  ingress:
  - conditions:
    - status: "True"
      type: Admitted
